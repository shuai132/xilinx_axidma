# 运行测试
mkdir xilinx_axidma
mount -t nfs -o nolock 192.168.2.101:/home/parallels/github/xilinx_axidma xilinx_axidma
cd xilinx_axidma && export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:.

cd outputs
insmod axidma.ko
insmod adc_ctrl.ko
cd arm-Debug
./app

rmmod axidma.ko

# uboot
setenv bootargs cma=256M

# NFS
# 修改配置
sudo gedit /etc/exports
添加目录例子，注意要使用绝对目录：
/home/parallels/nfs *(rw,sync,no_root_squash,insecure,no_subtree_check)
# 重启服务
sudo /etc/init.d/nfs-kernel-server restart
